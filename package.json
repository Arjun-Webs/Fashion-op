{
  "name": "aura-fashion-ai-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Full-Stack AURA Fashion AI Platform with Graph Analytics Engine and Next-Gen Social Shopping",
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "preview:frontend": "npm run preview --prefix frontend",
    "dev:backend": "cd backend && uvicorn main:app --reload --port 8000",
    "dev": "npx concurrently \"npm run dev:frontend\" \"npm run dev:backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
